<div class="container p-4">
	<div class="row">

		<div class="col-md-4 mx-auto"><!--con el comando mx-auto que pongo ahi estoy centrando el formulario entero-->
		<div class="card">
			<div class="card-body">

				<form action="/links/edit/{{enlace.id}}" method="POST"> <!-- Aca le digo que le paso el form a esa ruta concatenado con el id del enlace que le pase antes y con el metodo POST asi agarra el metodo router.pos() del documento links.js -->

					<div class="form-group">

						<input types="text" value="{{enlace.title}}" class="form-control" name="title" placeholder="Title" autofocus><!-- Aca le estoy diciendo que me muestre el titulo del enlace que le pase -->


					</div>
					<div class="form-group">
						<input type="url" value="{{enlace.url}}"name="url" class="form-control" placeholder="URL"> <!-- Aca le muestro la url del enlace que le pase -->
					</div>
					<div class="form-group">
						<textarea name="description" rows="2" class="form-control" placeholder="Descripcion">{{enlace.description}}</textarea> <!-- Aca le muestro la descripcion del enlace que le pase -->
					</div>
					<div class="form-group">
						<button class="btn btn-success btn-block">
							Save
						</button>
					</div>

				</form>

			</div>

		</div>	
		</div>

	</div>
</div>